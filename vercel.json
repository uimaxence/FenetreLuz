{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --prefix site",
  "buildCommand": "npm run build --prefix site && rm -rf .vercel/output && mkdir -p .vercel && cp -R site/.vercel/output .vercel/output",
  "framework": null
}
